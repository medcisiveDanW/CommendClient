package com.medcisive.commend;

/**
 *
 * @author vhapalchambj
 */
public class Commend {

    public static void main(String[] args) {
        if (args == null || args.length < 1) {
            System.out.println(" Client: no init file argument program exit ");
            System.exit(0);
        }
        CommendClient commend = new CommendClient(args, "Commend v1.2");
    }
}
